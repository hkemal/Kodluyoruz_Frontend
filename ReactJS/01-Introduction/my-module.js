export default (name) => {
  console.log(`hello  ${name}`);
};

const topla = (a, b) => a + b;

const cikar = (a, b) => a - b;

const text = `Text`;

const user = {
  name: "John",
  surname: "Doe",
};

const numArr = [1, 2, 3, 4];

export { topla, cikar, text, user, numArr };
